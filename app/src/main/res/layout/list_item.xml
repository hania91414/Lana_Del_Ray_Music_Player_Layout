<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_vertical_padding"
    android:paddingRight="@dimen/activity_vertical_padding"
    android:paddingTop="@dimen/activity_horizontal_padding"
    android:paddingBottom="@dimen/activity_horizontal_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/play_button"
            android:src="@drawable/icons8_play"
            android:backgroundTint="@color/transparent"
            style="@style/icon_style"/>

     <LinearLayout
         android:layout_width="0dp"
         android:layout_height="wrap_content"
         android:layout_weight="1"
         android:orientation="vertical">

    <TextView
        android:id="@+id/song_title"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_marginTop="@dimen/margin_top_list_items"
        android:paddingLeft="@dimen/padding_left_list_items"
        android:paddingRight="@dimen/padding_right_list_items"
        android:textSize="@dimen/song_title_size"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        tools:text="Love"
        tools:textColor="@android:color/black">
    </TextView>

    <TextView
        android:id="@+id/song_time"
        android:layout_height="match_parent"
        android:layout_width="wrap_content"
        android:layout_alignBottom="@id/song_title"
        android:textSize="@dimen/song_time_size"
        android:textColor="@android:color/white"
        android:layout_marginTop="@dimen/time_item_margin_top"
        android:paddingLeft="@dimen/padding_left_list_items"
        tools:text="@string/BTD1_time"
        tools:textColor="@android:color/black">
    </TextView>
     </LinearLayout>

        <ImageButton
            android:layout_height="@dimen/stop_icon_size"
            android:layout_width="@dimen/stop_icon_size"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:backgroundTint="@color/transparent"
            android:src="@drawable/icons8_stop" />
    </LinearLayout>

</LinearLayout>